import Image from "next/image"; import { siteConfig } from "@/config/site"; import type { Business, Department } from "@/data/content"; import { Badge, Button, Card } from "./ui";
export function BusinessCard({ business }: { business: Business }) { return <Card className="group overflow-hidden p-0"><div className="relative aspect-[16/9]"><Image src={business.image} alt={`${business.name} in NorthBridge`} fill sizes="(max-width: 768px) 100vw, 50vw" className="object-cover transition duration-700 group-hover:scale-[1.035]"/><div className="absolute inset-0 bg-gradient-to-t from-black/45 via-transparent to-black/15"/></div><div className="p-6"><div className="mb-4 flex items-center justify-between gap-2"><span className="eyebrow">{business.category}</span><Badge>{business.status}</Badge></div><h3 className="text-2xl font-black">{business.name}</h3><p className="mt-3 leading-6 text-zinc-400">{business.description}</p><p className="mt-4 text-xs text-zinc-500">{business.location}</p></div></Card>; }
export function DepartmentCard({ department }: { department: Department }) { return <Card className="flex flex-col overflow-hidden p-0"><div className="relative aspect-[16/7]"><Image src={department.image} alt={`${department.name} visual identity`} fill sizes="(max-width: 768px) 100vw, 50vw" className="object-cover"/></div><div className="flex flex-1 flex-col p-6"><div className="flex items-center justify-between"><span className="eyebrow">{department.short}</span><Badge>{department.status}</Badge></div><h3 className="mt-4 text-2xl font-black">{department.name}</h3><p className="mt-3 text-zinc-400">{department.description}</p><p className="mt-5 text-sm leading-6 text-zinc-500"><strong className="text-zinc-300">Expected:</strong> {department.expectations}</p><Button href={siteConfig.discordUrl} variant="secondary" className="mt-6 self-start">Apply on Discord</Button></div></Card>; }
