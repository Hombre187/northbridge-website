import { siteConfig } from "@/config/site"; import { getFallbackServerStatus } from "@/lib/server-status"; import { Badge, Button, Card } from "./ui";
export function ServerStatus() { const status = getFallbackServerStatus(); return <Card className="red-glow mx-auto max-w-5xl"><div className="grid items-center gap-6 sm:grid-cols-[1fr_auto_auto_auto]"><div><div className="flex items-center gap-3"><span className={`h-2.5 w-2.5 rounded-full ${status.online ? "bg-emerald-400" : "bg-zinc-500"}`}/><h2 className="text-xl font-black">NorthBridge City</h2><Badge>{status.online ? "Online" : "Offline"}</Badge></div><p className="mt-2 text-xs text-zinc-500">Last updated: {status.updatedAt}</p></div><div><p className="text-xs uppercase tracking-widest text-zinc-500">Players</p><p className="mt-1 text-xl font-black">{status.players} / {status.maxPlayers}</p></div><div><p className="text-xs uppercase tracking-widest text-zinc-500">Join code</p><p className="mt-1 font-mono text-sm">{siteConfig.serverAddress}</p></div><Button href={siteConfig.connectUrl}>Connect</Button></div></Card>; }
