import type { Metadata } from "next"; import Image from "next/image"; import { Container, PageHero } from "@/components/ui";
export const metadata: Metadata={title:"Gallery",description:"Scenes, vehicles, businesses, and events from NorthBridge RP."}; const gallery=["screenshots","businesses","departments","events","vehicles","city"];
export default function Gallery(){return <><PageHero eyebrow="City archive" title="Moments from NorthBridge." copy="A home for community screenshots, events, departments, businesses, and standout vehicles."/><section className="py-20"><Container><div className="grid auto-rows-[220px] gap-4 md:grid-cols-2 lg:grid-cols-3">{gallery.map((name,i)=><figure key={name} className={`group relative overflow-hidden rounded-lg border border-white/10 ${i===0||i===4?"lg:col-span-2":""}`}><Image src={`/images/gallery/${name}.svg`} alt={`${name} gallery placeholder`} fill sizes="(max-width: 768px) 100vw, 50vw" className="object-cover transition duration-500 group-hover:scale-105"/><figcaption className="absolute inset-x-0 bottom-0 bg-gradient-to-t from-black to-transparent p-5 pt-12 text-sm font-bold uppercase tracking-widest">{name}</figcaption></figure>)}</div></Container></section></>}
