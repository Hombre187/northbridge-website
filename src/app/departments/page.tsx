import type { Metadata } from "next"; import Image from "next/image"; import { DepartmentCard } from "@/components/cards"; import { Container, PageHero } from "@/components/ui"; import { departments } from "@/data/content"; import { northBridgeMedia } from "@/data/media";
export const metadata: Metadata={title:"Departments",description:"Explore public service and mechanic careers in NorthBridge RP."};
export default function Departments(){const scene=northBridgeMedia[2];return <><PageHero eyebrow="Careers" title="Answer the call." copy="Join a team that creates high-stakes, collaborative roleplay across the city."/><section className="py-20"><Container><div className="relative mb-8 aspect-[16/7] overflow-hidden rounded-[var(--radius-card)] border border-white/10"><Image src={scene.src} alt={scene.alt} fill sizes="100vw" style={{objectPosition:scene.position}} className="object-cover"/><div className="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-black/15"/></div><div className="grid gap-6 md:grid-cols-2">{departments.map(d=><DepartmentCard key={d.short} department={d}/>)}</div></Container></section></>}
