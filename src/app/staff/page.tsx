import type { Metadata } from "next"; import { Card, Container, PageHero } from "@/components/ui"; import staff from "@/data/staff.json";
export const metadata: Metadata={title:"Staff",description:"Meet the NorthBridge RP community team."};
export default function Staff(){return <><PageHero eyebrow="Community team" title="People behind the bridge." copy="A community-focused team responsible for operations, support, moderation, and development."/><section className="py-20"><Container><p className="mb-8 rounded border border-white/10 bg-white/5 p-4 text-sm text-zinc-400">These are initial or placeholder staff entries. Edit them in <code>src/data/staff.json</code> before launch.</p><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{staff.map(group=><Card key={group.group}><h2 className="text-2xl font-black">{group.group}</h2><div className="mt-5 space-y-3">{group.members.map(m=><div className="border-l-2 border-red-500/60 pl-4" key={`${group.group}-${m.name}`}><p className="font-bold">{m.name}</p><p className="text-sm text-zinc-500">{m.role}</p></div>)}</div></Card>)}</div></Container></section></>}
