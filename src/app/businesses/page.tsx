import type { Metadata } from "next"; import Image from "next/image"; import { BusinessCard } from "@/components/cards"; import { Card, Container, PageHero } from "@/components/ui"; import { businesses } from "@/data/content"; import { northBridgeMedia } from "@/data/media";
export const metadata: Metadata={title:"Businesses",description:"Discover player-led businesses in NorthBridge RP."};
export default function Businesses(){const businessMedia=northBridgeMedia.filter(item=>item.category==="Businesses");return <><PageHero eyebrow="Commerce & culture" title="Built by players." copy="NorthBridge businesses are social hubs, career paths, and foundations for stories across the city."/><section className="py-20"><Container><div className="mb-6 grid gap-4 md:grid-cols-2">{businessMedia.slice(0,2).map(item=><div className="group relative aspect-[16/9] overflow-hidden rounded-[var(--radius-card)] border border-white/10" key={item.src}><Image src={item.src} alt={item.alt} fill sizes="(max-width: 768px) 100vw, 50vw" className="object-cover transition duration-700 group-hover:scale-[1.035]"/><div className="absolute inset-0 bg-gradient-to-t from-black/65 via-transparent to-black/15"/></div>)}</div><div className="grid gap-6 md:grid-cols-2">{businesses.map(b=><BusinessCard key={b.name} business={b}/>)}</div><Card className="mt-6 text-center"><p className="eyebrow">More coming soon</p><h2 className="mt-3 text-2xl font-black">Bring your idea to NorthBridge.</h2><p className="mt-3 text-zinc-400">Future business proposals and application details will be announced in Discord.</p></Card></Container></section></>}
